#!/bin/bash

if [ ! -z $CONSUL_LOG_LEVEL ]; then
  log_level=$CONSUL_LOG_LEVEL
else
  log_level=warn
fi

/usr/local/bin/envconsul \
  -sanitize \
  -upcase \
  -log-level=$log_level \
  -consul=$CONSUL_HOST:8500 \
  $CONSUL_PREFIXES \
  "$@"
